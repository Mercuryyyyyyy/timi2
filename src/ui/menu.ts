import { CONTAINER_WIDTH, CONTAINER_HEIGHT } from '../constants';

export function drawMenu(_ctx: CanvasRenderingContext2D): void {}
export function isStartButtonClicked(_x: number, _y: number): boolean { return true; }
